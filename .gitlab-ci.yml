image: cirrusci/flutter:beta

stages:
  - generate
  - build
  - verify

generate:
  stage: generate
  before_script:
    - flutter pub get
  script:
    - flutter pub run build_runner build
  artifacts:
    paths:
      - lib/**/*.g.dart

build:
  stage: build
  script:
    - flutter build aot
  tags:
    - app
    - android

test:
  stage: verify
  script:
    - flutter test
  tags:
    - app
    - android

analyze:
  stage: verify
  script:
    - flutter analyze
  tags:
    - app
    - android
