name: generator
description: A web server to generate/update Gitlab repos
version: 1.0.0

publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  args: ^2.1.1
  dotenv: ^3.0.0
  path: ^1.8.0
  pretty_json: ^2.0.0
  shelf: ^1.1.4
  studyou_core:
    path: ../core
  supabase: ^0.0.1-dev.16

dev_dependencies:
  lint: ^1.5.3
  shelf_hotreload: ^0.0.2
