version: '3'
services:
  studyu-apps:
    build:
      context: ../../
      dockerfile: ./docker/hpi/Dockerfile
      args:
        ENV: selfhost
    ports:
      - 80:80
      - 443:443
    networks:
      - studyu-net
    env_file:
      - ../../flutter_common/lib/envs/.env.selfhost
