version: '3'
services:
  apps:
    build:
      context: ../../
      dockerfile: ./docker/hpi/Dockerfile
      args:
        ENV: selfhost
    ports:
      - 80:80
      - 443:443
    env_file:
      - ../../flutter_common/lib/envs/.env.selfhost
