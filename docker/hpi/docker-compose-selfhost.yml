version: '3'
services:
  apps:
    build:
      context: ../../
      dockerfile: ./docker/hpi/Dockerfile
      args:
        ENV: selfhost
    ports:
      - 80:80
      - 443:443
    env_file:
      - ../../flutter_common/lib/envs/.env.selfhost
    # extra_hosts:
      # - "app.hpi.studyu.health:127.0.0.1"
      # - "designer.hpi.studyu.health:127.0.0.1"
      # - "db.hpi.studyu.health:127.0.0.1"
#    volumes:
#      - ./etc/letsencrypt:/etc/letsencrypt
#      - ./certbot/data:/var/www/certbot

#  certbot:
#    container_name: certbot
#    image: certbot/certbot:latest
#    depends_on:
#      - apps
#    command: >-
#             certonly --reinstall --webroot --webroot-path=/var/www/certbot
#             --email ${EMAIL} --agree-tos --no-eff-email
#             -d ${DOMAIN}
#    volumes:
#      - ./etc/letsencrypt:/etc/letsencrypt
#      - ./certbot/data:/var/www/certbot
