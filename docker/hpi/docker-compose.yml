version: '3'
services:
  studyu-apps:
    build:
      context: ../../
      args:
        ENV: staging
    ports:
      - 80:80
      - 443:443
    env_file:
      - ../../flutter_common/lib/envs/.env.staging