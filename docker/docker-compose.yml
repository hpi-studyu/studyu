version: '3'
name: 'studyu'

services:
  apps:
    container_name: apps
    build:
      context: ../../
      dockerfile: ./docker/hpi/Dockerfile
      args:
        ENV: staging
    ports:
      - 80:80
      - 443:443
    env_file:
      - ../../flutter_common/lib/envs/.env.staging
