name: studyu_mcp
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"
resolution: workspace

dependencies:
  mcp_server: ^2.0.0
  studyu_validator:
    path: ../studyu_validator
  vm_service: ^15.2.0

dev_dependencies:
  test: ^1.30.0
