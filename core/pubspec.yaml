name: studyou_core
description: StudYou Models and common functions for app and study designer
version: 0.0.2

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter_dotenv: 2.1.0
  # Change back to main branch when we do not use back4app or issue is fixed: https://github.com/parse-community/Parse-SDK-Flutter/issues/490
  parse_server_sdk_flutter:
    git:
      url: git://github.com/parse-community/Parse-SDK-Flutter.git
      ref: a35fe3a86bfbdb0f2e6d864145b0202754776781
      path: packages/flutter
  quiver: ^2.1.5
  uuid: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^1.10.4
  effective_dart: ^1.3.0
  json_serializable: ^3.5.0

dependency_overrides:
  parse_server_sdk:
    git:
      url: git://github.com/parse-community/Parse-SDK-Flutter.git
      ref: a35fe3a86bfbdb0f2e6d864145b0202754776781
      path: packages/dart
