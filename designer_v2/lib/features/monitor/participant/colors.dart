import 'package:flutter/material.dart';

const Color incompleteColor = Color.fromARGB(255, 234, 234, 234);

// Add transparency to increase the readability of the text
Color partiallyCompleteColor = Colors.lightBlue.withValues(alpha: 0.75);

const Color completeColor = Colors.lightBlue;
